### タスクリスト：リマインダー通知機能

**テスト駆動開発（TDD）のアプローチ**

1.  **テストリストの作成**: これから作成するタスクリストがテストリストです。
2.  **テストの失敗**: 各タスクについて、まず機能が動作しないことを確認するテストを作成します。
3.  **実装**: テストが成功するように機能を実装します。
4.  **リファクタリング**: コードをクリーンにします。

---

#### 0. 準備 (Preparation)

-   [x] `feature/issue21-reminder` の作業ブランチを作成する。
-   [x] `script.test.js` に、リマインダー機能のテストスイート (`describe`) をセットアップする。

#### 1. データモデルの更新 (Data Model)

-   [x] **テスト**: タスクオブジェクトに `reminderRule` と `reminderAt` プロパティが追加・保存されることを確認するテストを作成する。
-   [x] **実装**: `addTask` 関数と `saveTasks` 関数を修正し、タスクオブジェクトが `reminderRule` (ユーザー設定) と `reminderAt` (通知絶対時刻) を保持できるようにする。
    -   `reminderRule` の例: `{ "type": "relative", "value": { "amount": 1, "unit": "hours" } }`
    -   `reminderAt` の例: `2025-08-20T10:00:00.000Z` (ISO文字列)

#### 2. リマインダー設定UIの実装 (UI)

-   [x] **UI実装**: `index.html` のタスク編集モーダル (またはタスク追加エリア) に、リマインダー設定用のUIを追加する。
    -   [x] 「絶対日時」と「相対日時」を選択するラジオボタンまたはセレクトボックス。
    -   [x] 絶対日時用の `datetime-local` 入力欄。
    -   [x] 相対日時用の数値入力欄と単位（分・時間・日）のセレクトボックス。
-   [x] **テスト**: UIで設定した値から、正しい `reminderRule` オブジェクトが生成されることを確認するテストを作成する。
-   [x] **実装**: UIの入力値を基に `reminderRule` オブジェクトを生成し、タスク保存時にオブジェクトに含めるロジックを実装する。
-   [x] **実装**: タスクの期日 (`dueDate`) が変更された際に、`reminderAt` が自動で再計算されるロジックを実装する。

#### 3. 通知機能のコアロジック (Notification Core)

-   [x] **実装**: アプリケーション起動時に、ブラウザに通知許可を要求する `Notification.requestPermission()` を呼び出す処理を追加する。
-   [x] **UI実装**: 通知が拒否された場合に、「通知をオンにしてください」というメッセージを画面上に表示する。
-   [x] **実装**: Service Worker (`service-worker.js`) を登録する処理を追加する。まだファイルがなければ新規作成する。

#### 4. 通知スケジューリング (Scheduling)

-   [x] **テスト**: `reminderAt` が過去の時刻になったタスクを正しく検出できることを確認するテストを作成する。
-   [x] **実装**: 定期的に (例: `setInterval` を使って1分ごとに) `localStorage` からタスクをチェックする関数を実装する。
-   [x] **実装**: `reminderAt` が現在時刻を過ぎており、まだ通知されていないタスクを検出するロジックを実装する。(通知済みフラグをタスクに追加する必要がある)
-   [x] **実装**: 通知対象のタスクを見つけたら、Service Worker にタスク情報を `postMessage` で送信する。

#### 5. Service Worker と通知表示 (Service Worker)

-   [x] **実装 (`service-worker.js`)**: メインスレッドからの `message` イベントを待ち受けるリスナーを追加する。
-   [x] **テスト (`service-worker.js`)**: 受け取ったタスク情報を基に、正しいタイトルと本文で通知が表示されることを確認するテストを作成する (テスト環境での Service Worker のテスト方法を要検討)。
-   [x] **実装 (`service-worker.js`)**: 受け取った情報 (`title`, `body`, `data`) を使って `self.registration.showNotification()` を呼び出し、デスクトップ通知を表示する。
-   [x] **実装 (`service-worker.js`)**: `notificationclick` イベントリスナーを追加し、通知がクリックされたときに `clients.openWindow()` でアプリケーションのタブを開き、フォーカスする処理を実装する。

#### 6. ドキュメント作成 (Documentation)

-   [x] `_docs/` に `2025-08-18_01_issue21_reminder.md` というファイル名で実装ログを残す。
